# crash_handler.py
class CrashHandler:
    def classify_crash(self, crash_data):
        # Classify crash severity
        return "Medium"

    def log_crash(self, crash_data, severity):
        # Log crash data
        print(f"Logged crash with severity: {severity}")
